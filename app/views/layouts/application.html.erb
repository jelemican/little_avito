<!DOCTYPE html>
<html>


</div>
  <head>
    <title>Store</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <% if user_signed_in? %>
    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <span class="my-0 mr-md-auto font-weight-normal" href = "#"> <%= link_to "Superstore", home_path %></span>
      <nav class="my-2 my-md-0 mr-md-3">
        <span class="p-2 text-dark" href="#"><%= link_to "Add a product", new_product_path %></span>
        <span class="p-2 text-dark"><%= link_to "About us", about_pages_path %></span>
      </nav>
        <span class="btn btn-outline-primary" href="#"><%= link_to "Sign out", destroy_user_session_path, method: :delete %></span>
    </div>
  <% else %>
    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <span class="my-0 mr-md-auto font-weight-normal" href = "#"> <%= link_to "Superstore", home_path %></span>
      <nav class="my-2 my-md-0 mr-md-3">
        <span class="p-2 text-dark" ><%= link_to "About us", about_pages_path %></span>
      </nav>
        <span class="p-2 text-dark" href="#"><%= link_to "Sign in", new_user_session_path %></span>
        <span class="btn btn-outline-primary" href="#"><%= link_to "Sign up", new_user_registration_path %></span>
    </div>
  <% end %>
  <div class = "container main">
    <div class = "row">
      <div class = "col-md-12">
        <% flash.each do |name, message| %>
          <div class = "alert <%= bootstrap_class_for(name) %>">
            <button class = "close" data-dismiss="alert">&times</button>
            <%= message %>
          </div>
        <% end %>
      </div>
    </div>
  <div class = "container">
    <%= yield %>
  </div>
  </body>
</html>




























